import React from "react";
import {AuthFooterStyled} from "./Auth.styled";

const AuthFooter = () => {
  return (
    <AuthFooterStyled>
      *Authorization on the territory of Russia and Belarus is possible only
      through a VPN
    </AuthFooterStyled>
  );
};

export default AuthFooter;
